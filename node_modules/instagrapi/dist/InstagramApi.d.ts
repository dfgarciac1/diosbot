import { TProfile, TLastPosts, TPost } from './types';
declare class InstagramApi {
    static getProfile(username: string): Promise<TProfile>;
    static getLastPosts(username: string): Promise<TLastPosts>;
    static getPost(postUrl: string): Promise<TPost>;
}
export { InstagramApi };
